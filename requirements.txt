# Python dependencies for Elastic SIEM CTF scripts

# Elasticsearch client
elasticsearch>=8.0.0,<9.0.0

# YAML parsing for credentials
pyyaml>=6.0

# Optional: for better performance monitoring
tqdm>=4.65.0